[package]
name = "omaboot-apply"
description = "Privileged installer for omaboot; publishes an already-staged, already-validated theme"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "omaboot-apply"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
clap.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints.rust]
unsafe_code = "forbid"
